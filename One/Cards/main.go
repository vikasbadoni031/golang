package main

import "fmt"

func main() {  //any program having a package main must have a main func, which gets executed when the executable is executed
	card := "Ace of spades" 
	card = "Five of diamonds"  //:= is only used durin the first assigment
	fmt.Println(card)
}